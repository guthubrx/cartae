# ============================================================================
# Cartae Database - Configuration Template
# ============================================================================
# Copie ce fichier vers .env et personnalise les valeurs
# cp .env.example .env

# ============================================================================
# PostgreSQL Configuration
# ============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=cartae
POSTGRES_USER=cartae
POSTGRES_PASSWORD=changeme_cartae_dev_password

# Pool connexions (optionnel)
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=5000

# ============================================================================
# pgAdmin Configuration
# ============================================================================
PGADMIN_PORT=5050
PGADMIN_EMAIL=admin@cartae.dev
PGADMIN_PASSWORD=changeme_admin_password

# ============================================================================
# Database API Configuration
# ============================================================================
# API Server
API_PORT=3001
NODE_ENV=development

# CORS (optionnel, * pour dev, domaine spécifique pour prod)
CORS_ORIGIN=*

# ============================================================================
# Sécurité (PRODUCTION)
# ============================================================================
# Pour production, génère des mots de passe forts avec:
# openssl rand -base64 32

# POSTGRES_PASSWORD=<généré avec openssl>
# PGADMIN_PASSWORD=<généré avec openssl>
