# Environment variables (contains secrets)
.env

# pgAdmin password file (contains credentials in clear text)
pgadmin-config/pgpass

# Backups (peuvent être gros)
backups/*.sql
backups/*.dump

# Logs
*.log

# OS
.DS_Store
Thumbs.db
